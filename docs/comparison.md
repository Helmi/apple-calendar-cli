# Why acal exists

1. Full CRUD and recurrence semantics in one local CLI.
2. Stable JSON envelope + exit-code contract for automation and agents.
3. Shell-native workflow — no server process, no MCP layer required.
